import 'package:flutter/material.dart';

class SellerCreatePropertyScreen extends StatelessWidget {
  const SellerCreatePropertyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
