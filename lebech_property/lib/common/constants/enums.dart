enum SearchType {
  none
}

enum SignInRouteType {
  normal,
  backScreen
}

enum ApplicationType {
  buyer,
  seller,
  broker,
  propertySeller
}

enum PropertyAvailability {
  immediately,
  selectDate
}

enum SeparateCleaningArea {
  none,
  yes,
  no
}